# QA — UI local

Issue: `3f9d35ae-a817-44c8-b95b-525440aa1320`  
Data: 2026-07-13  
Escopo assumido: árvore de trabalho atual (implementação ainda não commitada), pois a Issue não informa merge-base/commit de referência.

## Evidências executadas

| Verificação | Resultado |
|---|---|
| `npm test` | PASS — 48 testes |
| `npm run typecheck` | PASS |
| `npm run check:fitness` | PASS — 2 testes |
| `npm run build` | PASS |
| `git diff --check` | PASS |
| Smoke HTTP manual (`issues web --no-open`, `curl` em `/`, rota SPA e `/api/issues`) | PASS — listener em `127.0.0.1` |
| Teste em navegador/teclado assistido | NÃO EXECUTADO — não há Chromium/Chrome/Playwright/Puppeteer instalado no ambiente |

Contraste calculado sobre os tokens de `src/web/client/style.css`: texto primário 16.25:1, secundário 10.32:1, foco 8.45:1, erro 7.56:1, Status OPEN 7.33:1, CLAIMED 6.94:1, AWAITING 9.23:1 e CLOSED 8.16:1. Os pares de texto avaliados atendem AA; o contraste de borda de sucesso (3.14:1) não foi usado como único indicador.

## software-architect

- **Bloqueante:** `src/web/client/app.js:167-169` não mostra `claimed_at`, embora RF-UI-05 exija a data de Claim. Incluir a data condicional e testar uma Issue `CLAIMED`.
- **Bloqueante:** `src/web/client/app.js` tem 378 linhas, excedendo FF-03 de `DESIGN.md`; a fitness function só examina TypeScript. Separar responsabilidades e cobrir `src/web/client/*.js` na fitness function.
- **Julgamento:** falta teste HTTP que force salvamento obsoleto e valide o contrato `409`.

## qa-engineer

- **Bloqueante:** o diálogo é renderizado com `open` em `src/web/client/app.js:94-98`, sem `showModal()` ou contenção/gestão de foco; o foco alcança elementos atrás do diálogo, contrariando UX §9.
- **Bloqueante:** a busca chama `renderBoard()` a cada caractere (`app.js:205-213`), removendo o input ativo e perdendo o foco; não é utilizável apenas por teclado.
- **Julgamentos:** atualização esconde o quadro durante a requisição, contrariando UX §3; faltam cenários browser/DOM para foco, anúncio, retorno ao card e atualização.

## data-engineer

- Sem bloqueante de dados observado. A UI usa os casos de uso existentes; não há schema nem migração nova.
- **Julgamento:** falta teste integrado de concorrência CLI/UI que cause `Stale Issue save`, retorne `409` e confirme preservação do rascunho.

## security-engineer

- Sem bloqueante de acesso remoto observado: `src/web/server.ts` fixa `127.0.0.1`; o smoke HTTP confirmou o listener local.
- **Julgamentos:** teste deve afirmar o endereço efetivo do socket; `readBody` não possui limite de tamanho, permitindo consumo de memória por processo local.

## devops-engineer

- **Bloqueante:** falta documentação operacional da UI no `README.md` (início, URL/bind, porta e encerramento). O comando existe como `issues web`, mas não é documentado.
- **Julgamento:** o build emite testes em `dist/` e não limpa artefatos anteriores; falta separar build de produção e limpeza.
- **Julgamento:** não há CI/smoke operacional versionado; não bloqueia os critérios específicos desta Issue, mas reduz entregabilidade.

## Totais

- Bloqueantes: 6 (arquitetura: 2; qualidade/acessibilidade: 2; operação: 1; requisito de detalhe repetido entre arquitetura/qualidade contabilizado uma vez).
- Julgamentos: 7.
- Pior achado por eixo: arquitetura — ausência de data de Claim e limite de módulo; qualidade — teclado/foco; dados — cobertura de conflito; segurança — corpo sem limite; operação — falta de instruções de uso.

## Recomendação G3

- **reprovar** até que os bloqueantes sejam corrigidos e haja evidência de navegador para teclado/foco/diálogo.
- Sugestões de Issues `Implement`, independentes:
  1. `Corrigir acessibilidade de teclado e diálogo da UI local` — preservar foco na busca, tornar diálogo modal com trap/retorno de foco e cobrir em teste browser/DOM.
  2. `Completar detalhes e estrutura do client web` — exibir data de Claim, respeitar FF-03 e ampliar fitness/testes para o client JS.
  3. `Documentar e empacotar operação da UI local` — documentar `issues web`, URL/bind/ciclo de vida e ajustar build de produção limpo.

Os relatórios independentes das perspectivas estão em `.pi-subagents/artifacts/outputs/3cc8515c-33b5-40fc-aa38-1b3563a21ba1/qa/`.
